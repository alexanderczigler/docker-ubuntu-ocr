name: PR Check

on:
  pull_request:
    branches:
    - main

jobs:

  test:
    runs-on: ubuntu-latest
    steps:
      - name: 🛎️ Checkout
        uses: actions/checkout@v4

      - name: 🔧 Build
        uses: docker/build-push-action@v5
